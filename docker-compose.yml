services:
  frontend:
    build:
      context: ./frontend
    image: ${IMAGE_NAME:-camping-simulator-frontend}:${IMAGE_TAG:-local}
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-8080}:80"
